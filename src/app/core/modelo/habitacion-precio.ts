export interface HabitacionPrecio {
    habitacion: string,
    cantidadPersonas: number,
    aire?: boolean,
    esAdicional?: boolean,
    esNinio?: boolean,
    horas?: boolean,
    yacuzzi?: boolean,
    precio: number
}
